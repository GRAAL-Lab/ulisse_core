//AVOIDANCE CONF

centroid =
{
	latitude = 44.0956;
	longitude = 9.8631;
};

vehicle_data =
{
	rotational_speed = 0.0;	// set to 0 if A* should not increase cost for course change (assuming it takes 10s to turn pi rad)
	starting_velocity = 0.1;
	velocity_step = 0.1;

};

bounding_box = {
	gain = 0.0;
	min_distance_from_obs = 6.0; // count also asv size (circa 4m)
	reduction_while_checking_path = 1.0; // TODO this could depend only on the ASV capability of staying on the predicted course?
	safe_max_gap = 0.0; //min gap between safe and max bbs
	look_ahead_safety_span = 1.0; //seconds, affects keep out area in front of moving obstacles
};

planning = {
	only_once_on_same_vx = true;
    stop_search_if_goal_in_bb = true; // !! false -> infinite search || path is an aggroviglio around the goal
    same_position_threshold = 0.5;
    same_time_threshold = 0.1;

	status_pub_rate = 1.0;

	waypoint_acceptance_radius = 4.0;	// WARNING: high values makes checkPath fail when inside bb (probably when obs_gap ~< radius)

	check_progress_rate = 1.0; //Hz
	better_path_distance_perc = 0.9;   // new path has to be less than x% of current path
};






