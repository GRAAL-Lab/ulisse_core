// AVOIDANCE CONF

colregs = false; // ignore, set colregs-compliance from gui
centroid =
{
	latitude = 44.0956;
	longitude = 9.8631;
};

rotational_speed = 0.0;	// set to 0 if A* should not increase cost for course change (assuming it takes 10s to turn pi rad)

starting_velocity = 0.1;
velocity_step = 0.1;

better_path_distance_perc = 0.9;   // new path has to be less than x% of current path

check_progress_rate = 1.0;
status_pub_rate = 1.0;

waypoint_acceptance_radius = 8.0;	// WARNING: high values makes checkPath fail when inside bb (probably when obs_gap ~< radius)

max_pos_delay_time = 5.0;	// after x seconds without msg from NavFilter it stops
obs_expired_time = 5.0;	// after x seconds without msg from Radar it stops tracking that obs

bb_gain = 0.0;

min_distance_from_obs = 10.0; // count also asv size (circa 4m)
reduction_while_checking_path = 1.0; // TODO this could depend only on the ASV capability of staying on the predicted course?
safe_max_gap = 4.0; //min gap between safe and max bbs
look_ahead_safety_span = 5.0; //seconds, affects keep out area in front of moving obstacles
