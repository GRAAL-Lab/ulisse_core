// AVOIDANCE CONF

colregs = false; // actually add a button to the GUI
centroid =
{
	latitude = 44.0956;
	longitude = 9.8631;
};

rotational_speed = 0.314;	// set to 0 if A* should not increase cost for course change (assuming it takes 10s to turn pi rad)

starting_velocity = 0.1;

velocity_step = 0.1;

better_path_distance_perc = 0.8;   // new path has to be less than x% of current path

check_progress_rate = 1.0;
status_pub_rate = 1.0;

waypoint_acceptance_radius = 1.0;	// WARNING: high values makes checkPath fail when inside bb (probably when obs_gap ~< radius)

max_pos_delay_time = 5.0;	// after x seconds without msg from NavFilter it stops
obs_expired_time = 10.0;	// after x seconds without msg from Radar it stops tracking that obs
