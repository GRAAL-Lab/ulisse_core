
colregs = false; // actually update ulisse_map to read from here or add a button to the GUI
centroid =
{
	latitude = 44.0956;
	longitude = 9.8631;
};

rotational_speed = 0.314;

better_path_distance_perc = 0.8;   // new path has to be less than x% of current path

check_progress_rate = 0.5;
status_pub_rate = 1.0;

waypoint_acceptance_radius = 3.0;

max_pos_delay_time = 5.0;
obs_expired_time = 10.0;
